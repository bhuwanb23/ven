# `ven resolve`

Scans the project dependency graph, suggests resolutions for peer/pin/engine issues, and can apply **`npm install`** + **`ven.toml`** updates after confirmation.

```bash
ven resolve
```

See the in-app `ven resolve --help` for behavior details.
